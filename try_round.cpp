#include <iostream>
#include <stdlib.h>
#include <cmath>

using namespace std; 

int main()
{
    float a = -2.1; 
    cout <<"a "<<a<<" floor(a): "<<floor(a)<<" ceil(a): "<<ceil(a)<<" round(a): "<<round(a)<<endl;
    a = -2.8; 
    cout <<"a "<<a<<" floor(a): "<<floor(a)<<" ceil(a): "<<ceil(a)<<" round(a): "<<round(a)<<endl;
    a = 2.1; 
    cout <<"a "<<a<<" floor(a): "<<floor(a)<<" ceil(a): "<<ceil(a)<<" round(a): "<<round(a)<<endl;
    a = 2.8;
    cout <<"a "<<a<<" floor(a): "<<floor(a)<<" ceil(a): "<<ceil(a)<<" round(a): "<<round(a)<<endl;
    return 0;

}
